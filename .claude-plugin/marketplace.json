{
  "name": "agent-sync",
  "owner": {
    "name": "appvillis-com",
    "url": "https://github.com/appvillis-com"
  },
  "description": "Let concurrent coding agents share one project without colliding — leases, ID reservation, a run journal and a generated board, over a pluggable knowledge cloud.",
  "homepage": "https://github.com/appvillis-com/agent-sync",
  "repository": "https://github.com/appvillis-com/agent-sync",
  "plugins": [
    {
      "name": "agent-sync",
      "source": "./plugins/agent-sync",
      "description": "Coordination layer for multi-agent repositories. Gives task-pipeline runs a lease authority with TTL, race-free ID reservation, a cross-repo signal feed, an append-only run journal and a machine-generated read-only board, all decided by replaying one append-only log so no backend needs compare-and-swap. The knowledge backend is a pluggable adapter (Outline, filesystem) declaring its own capabilities, and degrades honestly to git-file leases when it cannot be the lease authority. Ships Claude Code hooks that deny edits to guarded registry files without a live lease.",
      "version": "0.5.0",
      "author": {
        "name": "appvillis-com"
      },
      "keywords": [
        "multi-agent",
        "coordination",
        "lease",
        "knowledge-base",
        "task-pipeline",
        "monorepo",
        "submodules"
      ],
      "category": "development"
    }
  ]
}
